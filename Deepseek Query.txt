You are an expert educator and front‑end developer. I will give you a technical topic and some reference materials. Your task is to create a **standalone, interactive HTML page** that explains the topic in a visually rich, infographic‑style manner.

**Inputs you will receive:**
- `[TOPIC]` : the technical subject (e.g., "GRU Hyperparameters")
- `[IMAGE_FILE]` : an image file name (optional; if provided, embed it)
- `[ADDITIONAL_NOTES]` : specific concepts to cover, data to include, etc.

The page must include:
1. A **header** with the topic title, a tagline, and a brief introduction.
2. An **infographic image display area** – embed the provided image with a fallback message if the image is missing (the fallback should contain a styled visual summary of the core structure/equations).
3. **Tabbed navigation** with at least these tabs:
   - “All Key Concepts” (or “All Hyperparameters”)
   - “Architecture / Structure”
   - “Training / Process”
   - “Regularization / Tuning”
   - “Full Code Example” (working Python example with all concepts shown)
   - “Summary & Comparison” (including a summary table, parameters vs hyperparameters comparison, and a common starting configuration)
4. For each individual concept (e.g., hyperparameter), create a **card** with:
   - A clear icon/emoji and name
   - A detailed, beginner‑friendly explanation
   - A **code snippet** showing how it is used in practice (e.g., Keras/PyTorch syntax)
   - Practical tips (highlighted boxes for warnings or good practices)
5. **If the topic involves mathematical equations:**
   - In the “Architecture / Structure” tab, provide a detailed breakdown of **every single element** of the core equations (e.g., what each symbol, subscript, operation, and matrix stands for). Use a separate card/box for each element, including its shape/dimensions and role.
   - Example breakdown format for an equation like `z_t = σ(W_z x_t + U_z h_{t-1})`:
     - `σ (sigmoid)` – logistic function, outputs (0,1); acts as a soft switch
     - `W_z` – weight matrix for current input, shape (hidden_units, input_features)
     - `x_t` – input vector at time t, shape (batch_size, input_features)
     - `U_z` – weight matrix for previous hidden state, shape (hidden_units, hidden_units)
     - `h_{t-1}` – previous hidden state, shape (batch_size, hidden_units)
     - `z_t` – update gate output, shape (batch_size, hidden_units)
   - Do this for **all** core equations, explaining every component.
6. The **Full Code Example** tab must contain a well‑commented, ready‑to‑run Python script that demonstrates all the concepts with clear hyperparameter annotations (like ①, ②, etc.). Use a realistic but self‑contained example (e.g., synthetic stock market data for GRU) and show key training/evaluation plots.
7. The **Summary & Comparison** tab must include:
   - A styled table of all covered concepts with purpose and typical ranges
   - A visual comparison between learned parameters and manually set hyperparameters
   - A “Common Starting Configuration” baseline card with recommended default values
8. Make the page **fully responsive** (works on mobile) with a modern, clean design using CSS custom properties, subtle shadows, rounded corners, and smooth animations.
9. Add a **back‑to‑top button** and ensure all tab switching is smooth with fade/slide animations.
10. Use a color palette of deep blue, teal, and light backgrounds.
11. Embed all CSS and JavaScript inline so the file works standalone.

**Output:** Only the complete, well‑commented HTML code, ready to be saved as an `.html` file and opened in a browser.

---

**Example usage:**  
`[TOPIC]` = GRU Hyperparameters  
`[IMAGE_FILE]` = ChatGPT Image May 12, 2026, 11_35_09 PM.png  
`[ADDITIONAL_NOTES]` = sample python code in detail with stock market example